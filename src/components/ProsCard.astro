---
const { title, desc, image } = Astro.props;
---

<div class="flex flex-col items-center">
  <div class="bg-slate-700 w-32 h-32 rounded-xl mb-6 border-shine">
    <img src={image} alt="" />
  </div>
  <h4 class="mb-4 text-center">{title}</h4>
  <p class="text-slate-400 text-center">{desc}</p>
</div>
